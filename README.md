# test-1 — Editor Hub
Seven fully paywall-safe editors vendored as submodules (MIT/Apache-2.0 only, UIs intact, licenses in-vendor).

Launch: index.html hub → editors/<name>/. Clone with --recurse-submodules.
Export: hub paywall — free 720p watermarked, Pro unlock (local flag) for clean 4K.
Engines: openreel-video shell, Clypra power core, freecut trim, clip-js WASM export, fabric canvas, AiCut timeline, cutflow captions.
Cloud-built via Actions + Pages.